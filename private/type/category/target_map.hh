#ifndef TYPE_CATEGORY_TARGET_MAP_HH
#define TYPE_CATEGORY_TARGET_MAP_HH

#include "type/category/target.hh"
#include <map>

namespace tester { namespace category {

typedef std::map<t_name, t_target> t_target_map;

}} // namespace tester::category

#endif
